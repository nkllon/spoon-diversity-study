# Design: SDD Setup v0.3

- Use `npx cc-sdd@latest --cursor --lang en`
- Keep commands in `.cursor/commands/kiro/`
- Provide `scripts/kiro` and Make targets
- Document in `docs/SDD_Tooling.md` and `AGENTS.md`

