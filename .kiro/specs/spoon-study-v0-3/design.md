# Design: Spoon Study v0.3

- Ontology: PROV‑O/SOSA/schema.org aligned; TTL in repo root
- Dataset: CSV with fixed header; dictionary generated by helper
- Diagrams: PUML (workflow), MMD (analysis), DOT (hierarchy)
- Validation: `make spec-validate` (shell checks), `make spec-sync` (dictionary)


